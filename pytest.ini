[pytest]
testpaths = tests
markers =
    smoke: end-to-end smoke tests that exercise real LLM/network (informational; failures don't block CI). Run explicitly with `pytest -m smoke`; deselect with `pytest -m "not smoke"`.
